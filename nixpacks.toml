[phases.setup]
nixPkgs = ['python39']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "No build step needed"']

[start]
cmd = 'python -m uvicorn railway_test_api:app --host 0.0.0.0 --port $PORT'
