[phases.setup]
nixPkgs = ["python39"]

[phases.install]
cmds = ["python -m ensurepip --upgrade", "python -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.a3e.main:app --host 0.0.0.0 --port $PORT"
